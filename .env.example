# Copy to .env.local for local dev (never commit real keys).
# Vite only exposes vars that start with VITE_ (NEXT_PUBLIC_ will not work here).

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_or_anon_key_here
